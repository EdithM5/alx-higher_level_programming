#!/usr/bin/python3
"""
defining the function
"""


def is_same_class(obj, a_class):
    """
    the fuction will return:
    true or false
    if the object is exactly instance/not
    """
    return (type(obj) == a_class)
