#!/usr/bin/python3
"""
defining the function
"""


def is_kind_of_class(obj, a_class):
    """
    the fuction will return:
    true or false
    if the object is like instance/not
    """
    return (isinstance(obj, a_class))
